# Micro Task Queue  
- call back queue get Promise(call back work) after the event complete in web Api MORE PRIORITY, So create a new Queue that is called Micro task Queue.
# JSON
- JSON  is a format , not a datatyope, but json data type is string.
- arry,object,.. convert into string Can use JSON. 
- Not Convert into(skipped) Function,undefind,NaN.
- JSON is the Medium for Data transfer.
# Web Architecture
- Front End -> convet to JSON format 
- Back End -> convert to JSON format
- Database -> Get data in database can use json format.
- advantages of JSON
    - different back end deifferent front end -> Independent
    - different back end multiple database -> Micro serivice Architecture
    - Reuse -> Platform Indepent(like windows, Linux, android..)
# Micro Service Architecture
- think like one database but multiple banckend not need different database.
